import React from 'react';
import {
    Typography,
    Container, Box, Grid, Card, CardContent, CardActions, Button
} from '@mui/material';

export default function MobileComponent() {
    return (
        <Typography variant="h3" sx={{ color: "#2061AE" }}>This is Mobile Component</Typography>
    )
}
