import React from 'react';
import {
    Typography,
    Container, Grid, Card, CardContent, CardActions, Button
} from '@mui/material';

export default function TabletComponent() {
    return (
        <Typography variant="h3" sx={{ color: "#2061AE" }}>This is Small Screen Component</Typography>
    )
}
